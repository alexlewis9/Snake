#ifndef CONSTANTS_H
#define CONSTANTS_H

const int WINDOW_SIZE = 1200;
const int WINDOW_WIDTH = WINDOW_SIZE;
const int WINDOW_HEIGHT = WINDOW_SIZE;
const int UNIT_SIZE = 60;
const int GRID_WIDTH = WINDOW_WIDTH / UNIT_SIZE;
const int GRID_HEIGHT = WINDOW_HEIGHT / UNIT_SIZE;
const enum directions { up, down, right, left };

#endif